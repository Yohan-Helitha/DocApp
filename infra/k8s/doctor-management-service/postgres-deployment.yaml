apiVersion: apps/v1
kind: Deployment
metadata:
  name: doctors-postgres
  labels:
    app: doctors-postgres
spec:
  replicas: 1
  selector:
    matchLabels:
      app: doctors-postgres
  template:
    metadata:
      labels:
        app: doctors-postgres
    spec:
      containers:
        - name: postgres
          image: postgres:15
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_USER
              valueFrom:
                configMapKeyRef:
                  name: doctor-db-config
                  key: PGUSER
            - name: POSTGRES_DB
              valueFrom:
                configMapKeyRef:
                  name: doctor-db-config
                  key: PGDATABASE
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: doctor-db-secret
                  key: PGPASSWORD
          volumeMounts:
            - name: postgres-data
              mountPath: /var/lib/postgresql/data
      volumes:
        - name: postgres-data
          persistentVolumeClaim:
            claimName: doctors-postgres-pvc

---
apiVersion: v1
kind: Service
metadata:
  name: doctors-postgres
spec:
  type: LoadBalancer
  selector:
    app: doctors-postgres
  ports:
    - name: internal
      protocol: TCP
      port: 5432
      targetPort: 5432
    - name: external
      protocol: TCP
      port: 5433
      targetPort: 5432

---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: doctors-postgres-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
