apiVersion: apps/v1
kind: Deployment
metadata:
  name: symptom-checker-postgres
  labels:
    app: symptom-checker-postgres
spec:
  replicas: 1
  selector:
    matchLabels:
      app: symptom-checker-postgres
  template:
    metadata:
      labels:
        app: symptom-checker-postgres
    spec:
      containers:
        - name: postgres
          image: postgres:15
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_USER
              valueFrom:
                configMapKeyRef:
                  name: symptom-checker-db-config
                  key: PGUSER
            - name: POSTGRES_DB
              valueFrom:
                configMapKeyRef:
                  name: symptom-checker-db-config
                  key: PGDATABASE
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: symptom-checker-db-secret
                  key: PGPASSWORD
          volumeMounts:
            - name: postgres-data
              mountPath: /var/lib/postgresql/data
            - name: symptom-checker-db-init
              mountPath: /docker-entrypoint-initdb.d
              readOnly: true
      volumes:
        - name: postgres-data
          persistentVolumeClaim:
            claimName: symptom-checker-postgres-pvc
        - name: symptom-checker-db-init
          configMap:
            name: symptom-checker-db-init
            items:
              - key: init.sql
                path: init.sql

---

apiVersion: v1
kind: Service
metadata:
  name: symptom-checker-postgres
spec:
  type: LoadBalancer
  selector:
    app: symptom-checker-postgres
  ports:
    - name: internal
      protocol: TCP
      port: 5432
      targetPort: 5432
    - name: external
      protocol: TCP
      port: 5440
      targetPort: 5432

---

apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: symptom-checker-postgres-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
