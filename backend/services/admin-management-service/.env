NODE_ENV=development
PORT=4008

# Reuse shared Postgres instance from docker-compose
PGHOST=postgres
PGPORT=5432
PGUSER=postgres
PGPASSWORD=postgres
PGDATABASE=authdb

# Or use a single DATABASE_URL value
DATABASE_URL=postgresql://postgres:postgres@postgres:5432/authdb

LOG_LEVEL=debug
